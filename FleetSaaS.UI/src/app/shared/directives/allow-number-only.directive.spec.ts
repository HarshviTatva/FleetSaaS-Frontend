import { AllowNumberOnlyDirective } from './allow-number-only.directive';

describe('AllowNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
